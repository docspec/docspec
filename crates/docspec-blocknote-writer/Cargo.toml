[package]
name = "docspec-blocknote-writer"
version = "1.0.2"
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "DocSpec event stream to BlockNote JSON writer"
keywords = ["document", "conversion", "streaming"]
categories = ["encoding"]

[dependencies]
base64 = "0.22"
docspec-core = { path = "../docspec-core", version = "1.0.0" }
docspec-json = { path = "../docspec-json", version = "1.0.0" }

[dev-dependencies]
docspec-markdown-reader = { path = "../docspec-markdown-reader", version = "1.0.0" }
serde_json = "1"

[lints]
workspace = true
