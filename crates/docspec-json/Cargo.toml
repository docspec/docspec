[package]
name = "docspec-json"
version = "1.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "JSON writing primitives for docspec writers"
keywords = ["document", "conversion", "streaming"]
categories = ["encoding"]

[lints]
workspace = true

[dependencies]
docspec-core = "0.5.0"
struson = "0.7"
